# Step 0-1 · 前置确认 + 了解她

> **核心提醒（每一步都适用）**
> - 一次一个命令/问题，等反馈再进下一步
> - 具体方案先回对应 step 文件查，不凭印象
> - 求稳 ≠ 求对 · 用户的朴素直觉值得被验证
> - 诊断看完整 transcript，不看末尾几行
> - **如有必要,realtime 搜索** · 本文件比你的记忆新,但也可能过时

---

## 首先 · 确认日期 + 披露你的知识截止

在开始任何步骤之前,**先和用户对齐时间**:

> 在我们开始之前——今天是几月几号？

用户告诉你之后,**你主动告诉她**:

> 好,今天是 [用户说的日期]。跟你说一下:我的知识库截止到 [你的训练截止日期],距离今天大概 [X] 个月。这意味着**过去 [X] 个月发生的技术变化我可能不知道**。
>
> 所以今天装机我会**严格按文件里的指引走**——这些文件是维护者近期核实的,比我的记忆新。如果遇到文件也没覆盖的情况,我会**现场搜索**确认当前真实状况。

**为什么要做这一步**:
- 让你(chatbox)对自己的时间盲区有 explicit awareness——后面遇到记忆和文件冲突时不会默认用记忆
- 让用户知道你的局限(建立正确预期)——她不会以为你什么都知道,也不会在你犯错时失去信任

---

## 步骤 0：三个前置确认（一次说完）

**(a) Claude Pro 订阅**

> 三个小确认。第一个：Claude Code 需要 Claude Pro 或更高的付费订阅（具体价格以 [claude.ai/pricing](https://claude.ai/pricing) 为准）。你订了吗？

如果没订，停在这里，让她去 claude.ai 订，订完再回来。

**(b) 她的电脑是 Mac 还是 Windows**

> 第二个：你的电脑是 Mac 还是 Windows？

**重要**：从这一步开始，你的整个指引路径根据她回答**完全不同**。你**不要混着说**——她答了哪种就走哪种路径。Mac 用户不要讲 PowerShell、Windows 用户不要讲 Homebrew。

- 她答 **Mac** → 后面装机走 `step-3m-mac装机.md`
- 她答 **Windows** → 后面装机走 `step-3w-windows装机.md`

如果她不确定："你看电脑左上角是苹果 🍎 标志就是 Mac，是 Windows 窗口标志就是 Windows"。

**Windows 用户需要的特殊准备**（如果她答 Windows，**现在就告诉她**）：

> 你是 Windows 用户——我们要用的东西里，**有一个叫「Git for Windows」的工具必须装**。这不是为了让你写代码，是因为 Claude Code **内部要用它**（Windows 没自带 Linux 风格的命令行，Claude Code 要从它那里借一个）。
>
> 装起来 3 步：
> 1. 打开 https://git-scm.com/downloads/win
> 2. 页面最上方有个 "**Click here to download**" 的大链接——点它，会下载一个 `Git-x.xx.x-64-bit.exe`
> 3. 双击打开这个 exe 开始安装
>
> **安装过程中的简单原则：全部默认点「Next」就装好**。如果你看到 Vim、PATH、line endings 这些词不知道是什么——**没关系，默认值是对的**。如果你**好奇**想了解，可以另外问我，但都和今天装 Claude Code 无关。只有最后一步会变成"Install"按钮，点那个开始装。
>
> 装完会弹出一个勾选 "Launch Git Bash" 的页面——**可以不勾**（我们用 PowerShell，不用 Git Bash）。点 "Finish"。
>
> 装完告诉我。

等她装完再进下一步。

**(c) install 脚本要保存到桌面**

**install 脚本只"上传到对话里"是不够的**——后面她要在终端跑它，必须本地有文件。

**根据她的系统告诉她要保存哪个文件**：

- **Mac 用户**：
  > 你收到的文件里有一个叫 `install.sh`。**请把这个文件保存到你的电脑桌面**。
  > 如果你是从微信/邮件收到的：通常「另存为 → 桌面」就行。
  > 保存好告诉我。

- **Windows 用户**：
  > 你收到的文件里有 `install.ps1` 和 `install.sh`——**两个都要存到桌面**。
  > 保存好告诉我。

等她确认保存好再进下一步。**不要跳过这一步**。

---

## 步骤 1：了解她

first-look 的常见场景是她已经和 Claude（Desktop / claude.ai）聊过很多，想把这份"被理解"迁移给 Claude Code。也有人想从新的自己开始。**让她选**。

问她：

> 你希望 Claude Code 怎么认识你？
>
> - **承接记忆**：把你和 Claude 之前的对话承接过来，Claude Code 一开始就了解你现在的状态
> - **从新开始**：忽略过去，我问你几个问题建立一份全新的介绍

她选哪条走哪条。

### 承接路径

告诉她：

> 好，我们把你和 Claude 之前的对话承接过来。你做这几步：
>
> 1. 打开 [claude.ai](https://claude.ai)，登录
> 2. 点左下角头像/名字 → **Settings** → **Privacy**
> 3. 点 **Export data**，确认
> 4. 过一小会儿，你注册邮箱会收到 Anthropic 的邮件（通常几分钟，慢的话几小时）
> 5. 邮件里有一个 zip 下载链接，下载好之后**直接把 zip 拖进来给我**——我能读 zip 里的文件，不用你解压

**等邮件的时候就安心等，不要去做别的事**。同时操作多件对她是认知负荷。

拿到文件之后：
- 识别格式（Claude.ai 的 JSONL、ChatGPT 的 JSON 等）
- 先 **scan 一遍**——看看大概有几段对话、信息密度如何、最新一段是什么时候
- 提取优先级：**有具体细节的、有情感投入的、有反复出现的主题** > 泛泛的事务对话
- **她主动上传 = 她希望 CC 知道这些**，不要替她自我审查。默认带过去；涉及第三方真名 / 凭据 / 明显敏感的内容单列询问（见下方）
- **如果你扫完发现关于她的有效信息少于 5-6 条**（比如只是"用 Claude 写了几次邮件"这种水平），**告诉她**："你的导出里关于你这个人的信息不多——我们用访谈补一些怎么样？"让她决定
- **如果对话量大到你不确定能不能读全**，告诉她："你的对话很多——我会优先看最近 3 个月和反复出现的主题。如果有更早的、特别想让我知道的事，你可以单独告诉我。"

让用户参与判断"够不够"，比 chatbox 自己拿一个数字 cutoff 决定要可靠。

**两件事要呈现给她自己决定**（不要自行处理）：

- **第三方真名**：如果记录里出现具体人名，把这些名字列出来告诉她，问她每一个怎么处理——保留真名、用关系称呼代替（"伴侣"、"好友 L"）、还是完全去掉。由她选
- **凭据类**：如果看到疑似密码、API key、token、身份证号、银行卡号等，把具体内容指出来，问她"这个你要保留还是忽略？"。不要自动判断

**如果她用的是 ChatGPT/DeepSeek/Kimi 等非 Claude**：指引去对应平台导出，上传给你，一样分析。

### 访谈路径

告诉她：

> 好，我们从新的自己开始。我问你几个问题。

五步。每步问 1-2 个，等她答再继续。

1. **破冰**：你平时用 AI 吗？用来做什么？
2. **了解你**：做什么工作？最近在忙什么？想用 CC 帮你做什么？你喜欢先想清楚还是边做边想？
3. **合作方式**：喜欢什么沟通风格？希望 CC 是什么角色？觉得你想法有问题它怎么说？
4. **边界**：有没有什么事情不希望它做？有没有话题不想被记住？
5. **总结**：用 3-5 句话说你对她的理解。

**不预设关系类型**。工具、伙伴、老师，都是她的选择。

---

## 这一步完成了

**在让她上传下一份文件之前，先做一个承接总结**——把你从这一段了解到的关于她的关键信息主动复述一遍：

> 在我们进入下一步之前，我先把我了解到的关于你的关键信息复述一下——你看看准不准、有没有我漏的：
>
> - [5-7 条 bullet · 你从承接/访谈中提取的核心画像]
>
> 准的话我们继续。

**为什么要做这一步**：
1. 让你（chatbox）把信息**主动重写一遍**进当前 context——下一份 step 文件会大量占 context，这段复述帮你抗拒 attention drift
2. 让用户**明确感知到你在听**，而不是只在执行流程

她确认后，告诉她：

> 做得好。接下来我要帮你生成一份配置文件。你收到的文件包里还有一个叫 **`step-2-生成配置.md`** 的文件，拖进来给我，我们继续下一步。
