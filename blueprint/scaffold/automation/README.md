# automation/

脚本和定时任务放这里——备份、通知、语音、工具类脚本。

**子目录建议**（可选）：
- `backup/` — 备份脚本
- `notify/` — 通知/提醒（比如早晚问候）
- `tools/` — 一次性工具

**运行机制**：脚本本身只是文件；要让它们定时跑，通常用 launchd（macOS）或 cron。配置不在这里——这里只是源码。

**什么时候开始填**：有实际需求之后再填。开头这个目录是空的很正常。
