# commands/ · 自定义 slash command

`/check-in`、`/quick-save` 这种——你在 CC 里打斜杠就能调的命令。

**CC 硬约束**：`commands/` 必须**扁平**，每个 `.md` 一个命令。

**和 skill 的区别**（模糊的）：
- **command** 偏向**主动调用**（你打 `/xxx`）
- **skill** 偏向**被动被 CC 选择执行**（根据上下文）

实际上很多时候两者做同一件事——选你喜欢的形式。

**什么时候做**：同 skill——有了明确的"这件事我想快速触发"之后再写。
